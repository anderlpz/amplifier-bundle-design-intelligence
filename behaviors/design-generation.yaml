bundle:
  name: design-generation-behavior
  version: 1.0.0
  description: Generation capability that produces Design Protocol-compliant artifacts and transforms design decisions into machine-readable tokens, specs, and export packages.

agents:
  include:
    - design-studio:token-generator
    - design-studio:spec-writer
    - design-studio:export-packager

context:
  include:
    - design-studio:context/generation-instructions.md
